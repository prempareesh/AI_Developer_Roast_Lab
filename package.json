{
    "name": "ai-developer-roast-lab",
    "version": "1.0.0",
    "description": "Root project running frontend and backend",
    "scripts": {
        "frontend": "npm run dev --prefix frontend",
        "backend": "npm run dev --prefix backend",
        "dev": "concurrently \"npm run backend\" \"npm run frontend\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}